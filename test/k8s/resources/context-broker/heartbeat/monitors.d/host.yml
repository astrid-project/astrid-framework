- type: icmp
  name: context-broker
  id: host@context-broker
  enabled: false
  schedule: "@every 60s"
  hosts:
    - "context-broker"
  ipv4: true
  ipv6: true
  mode: any
  timeout: 16s
  wait: 1s
  tags:
    - "heartbeat"
    - "ssh-server"
