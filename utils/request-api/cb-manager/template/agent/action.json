{
    "id": "{{agent_instance_id}}",
    "operations": [
        {
            "actions": [
                {
                    "id": "{{action_id}}"
                }
            ]
        }
    ]
}
