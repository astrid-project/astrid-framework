{
    "id": "agent-probe",
    "partner": "cnit",
    "actions": [
        {
            "id": "kafka-topic",
            "config": {
                "cmd": "sed -i 's/topic_id => \".*\"/topic_id => \"{value}\"/' /usr/share/logstash/pipeline/polycube_*.conf"
            }
        }
    ]
}
